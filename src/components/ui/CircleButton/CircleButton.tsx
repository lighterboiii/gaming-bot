import { FC } from "react";
import styles from './CircleButton.module.scss';

const CircleButton: FC = () => {
  return <div className={styles.button}>Circle Button</div>
}

export default CircleButton;