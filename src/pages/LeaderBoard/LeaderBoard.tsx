import { FC } from "react";
import styles from './LeaderBoard.module.scss';

const LeaderBoard: FC = () => {
  return <div className={styles.leaderBoard}>Leader Board</div>
}

export default LeaderBoard;